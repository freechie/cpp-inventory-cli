`cpp-inventory-cli` is a C++20 command-line application for managing inventory records—including quantities, prices, descriptions, and dates—with validated CSV persistence.

The goal is a portfolio-ready CRUD tool with safe terminal input, search and deletion, clean modular architecture, automated tests, sanitizers, documentation, and GitHub Actions CI.
